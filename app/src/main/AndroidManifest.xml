<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.katecatlin.detroitlive" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".DetroitLive"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.MainActivity_"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.NoInternetActivity_"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".activities.AddConcertActivity_"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".activities.abstractactivities.ConcertPagerActivity_"
            android:label="@string/title_activity_concert_pager" >
        </activity>
    </application>

</manifest>
